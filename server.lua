-- Version check only. The script itself is client-only; this server file exists
-- purely so ox_lib can compare the version in fxmanifest.lua against the latest
-- GitHub release and warn in the server console when outdated.
lib.versionCheck('xWennYs/orvar_props')
